<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>io.github.lexaquila</groupId>
    <artifactId>lyradb-backend</artifactId>
    <version>${revision}</version>
    <name>LyraDB Backend</name>
    <description>LyraDB 天琴智库 - 轻量 AI 通用数据库管理工具 - 后端</description>

    <properties>
        <!-- CI 通过 -Drevision=<tag> 覆盖，作为后端包与桌面包的单一版本源。 -->
        <revision>3.0.0</revision>
        <java.version>17</java.version>
        <maven-resolver.version>1.9.18</maven-resolver.version>
        <jasypt.version>3.0.5</jasypt.version>
        <h2.version>2.2.224</h2.version>
        <jsqlparser.version>5.3</jsqlparser.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Data JPA (for connection config persistence) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- 数据库结构迁移：生产环境不再依赖 Hibernate 自动建表 -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>

        <!-- SQL AST：企业授权、审批和脱敏必须基于完整语法树并失败关闭 -->
        <dependency>
            <groupId>com.github.jsqlparser</groupId>
            <artifactId>jsqlparser</artifactId>
            <version>${jsqlparser.version}</version>
        </dependency>

        <!-- Spring Boot Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Spring Boot WebSocket (for future streaming results) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- H2 Database (embedded storage for connection configs) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Maven Resolver API (dynamic driver download) -->
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-api</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-spi</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-util</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-supplier</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-connector-basic</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-transport-file</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-transport-http</artifactId>
            <version>${maven-resolver.version}</version>
        </dependency>

        <!-- Maven Aether (for Maven Resolver provider) -->
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>3.9.6</version>
        </dependency>

        <!-- Jasypt (credential encryption) -->
        <dependency>
            <groupId>com.github.ulisesbocchio</groupId>
            <artifactId>jasypt-spring-boot-starter</artifactId>
            <version>${jasypt.version}</version>
        </dependency>

        <!-- Jackson (already included via Spring Boot, explicit for date handling) -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Apache POI (Excel 导出) -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.2.5</version>
        </dependency>

        <!-- OpenCSV (for CSV export) -->
        <dependency>
            <groupId>com.opencsv</groupId>
            <artifactId>opencsv</artifactId>
            <version>5.9</version>
        </dependency>

        <!-- Apache MINA SSHD (SSH 隧道 / 跳板机端口转发, PRD F9) -->
        <dependency>
            <groupId>org.apache.sshd</groupId>
            <artifactId>sshd-core</artifactId>
            <version>2.12.1</version>
        </dependency>

        <!-- Spring Security (企业多用户基础，dev 默认关闭，prod 启用 HTTP Basic) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>aliyun</id>
            <name>Aliyun Public Repository</name>
            <url>https://maven.aliyun.com/repository/public</url>
        </repository>
        <repository>
            <id>maven-direct</id>
            <name>Maven Central Direct</name>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>aliyun-plugin</id>
            <name>Aliyun Plugin Repository</name>
            <url>https://maven.aliyun.com/repository/public</url>
        </pluginRepository>
    </pluginRepositories>

    <profiles>
        <!--
            桌面打包（PRD F10）：mvn -Pdesktop package
            使用 jpackage 生成自带精简 JRE 的本地应用镜像（app-image，无需 WiX）。
            前置条件：frontend/dist 已复制到 src/main/resources/static（由 package-desktop 脚本完成）。
            如需安装包（msi/dmg/deb），将 type 参数改为对应类型并安装 WiX（Windows）/ dmg 提示。
        -->
        <profile>
            <id>desktop</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <echo>Building desktop app-image via jpackage...</echo>
                                        <!-- JPackage 输入与输出必须隔离，避免递归打包自身输出。 -->
                                        <delete dir="${project.build.directory}/jpackage-input"/>
                                        <delete dir="${project.build.directory}/desktop"/>
                                        <mkdir dir="${project.build.directory}/jpackage-input"/>
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                              todir="${project.build.directory}/jpackage-input"/>
                                        <mkdir dir="${project.build.directory}/desktop"/>
                                        <exec executable="${java.home}/bin/jpackage" failonerror="true">
                                            <arg value="--type"/>
                                            <arg value="app-image"/>
                                            <arg value="--input"/>
                                            <arg value="${project.build.directory}/jpackage-input"/>
                                            <arg value="--dest"/>
                                            <arg value="${project.build.directory}/desktop"/>
                                            <arg value="--name"/>
                                            <arg value="LyraDB"/>
                                            <arg value="--main-jar"/>
                                            <arg value="${project.build.finalName}.jar"/>
                                            <arg value="--main-class"/>
                                            <arg value="io.github.lexaquila.lyradb.LyraDbApplication"/>
                                            <arg value="--app-version"/>
                                            <arg value="${revision}"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Xmx512m"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Dapp.desktop.tray-enabled=true"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Dspring.profiles.active=desktop"/>
                                            <arg value="--verbose"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
            Windows MSI 安装包：mvn -Pdesktop-msi package
            前置：安装 WiX Toolset（https://wixtoolset.org）并设 WiX 环境变量；frontend/dist 已嵌入 static。
            产物：target/desktop/LyraDB-${revision}.msi
        -->
        <profile>
            <id>desktop-msi</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-msi</id>
                                <phase>package</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <echo>Building Windows MSI via jpackage (needs WiX)...</echo>
                                        <!-- JPackage 输入与输出必须隔离，避免递归打包自身输出。 -->
                                        <delete dir="${project.build.directory}/jpackage-input"/>
                                        <delete dir="${project.build.directory}/desktop"/>
                                        <mkdir dir="${project.build.directory}/jpackage-input"/>
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                              todir="${project.build.directory}/jpackage-input"/>
                                        <mkdir dir="${project.build.directory}/desktop"/>
                                        <exec executable="${java.home}/bin/jpackage" failonerror="true">
                                            <arg value="--type"/>
                                            <arg value="msi"/>
                                            <arg value="--input"/>
                                            <arg value="${project.build.directory}/jpackage-input"/>
                                            <arg value="--dest"/>
                                            <arg value="${project.build.directory}/desktop"/>
                                            <arg value="--name"/>
                                            <arg value="LyraDB"/>
                                            <arg value="--main-jar"/>
                                            <arg value="${project.build.finalName}.jar"/>
                                            <arg value="--main-class"/>
                                            <arg value="io.github.lexaquila.lyradb.LyraDbApplication"/>
                                            <arg value="--app-version"/>
                                            <arg value="${revision}"/>
                                            <arg value="--vendor"/>
                                            <arg value="LyraDB"/>
                                            <arg value="--win-menu"/>
                                            <arg value="--win-shortcut"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Xmx512m"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Dapp.desktop.tray-enabled=true"/>
                                            <arg value="--java-options"/>
                                            <arg value="-Dspring.profiles.active=desktop"/>
                                        </exec>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
