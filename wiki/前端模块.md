# 前端模块说明

## 概述

前端基于 Vue 3.4 + TypeScript + Vite 5 构建，使用 Element Plus 作为 UI 组件库，Monaco Editor 作为 SQL 编辑器，VXE Table 展示数据表格，Pinia 管理全局状态，vue-i18n 提供中英文国际化。

## 目录结构

```
frontend/src/
├── api/              # API 调用层（Axios 封装）
│   ├── index.ts      # Axios 实例 & 拦截器
│   ├── app.ts        # 应用信息接口
│   ├── auth.ts       # 认证接口
│   ├── connection.ts # 连接管理接口
│   ├── driver.ts     # 驱动管理接口
│   ├── ent.ts        # 企业版接口
│   ├── history.ts    # 查询历史接口
│   ├── metadata.ts   # 元数据接口
│   ├── migration.ts  # 数据迁移接口
│   ├── task.ts       # 后台任务接口
│   └── report.ts     # 定时报表订阅接口
├── components/
│   ├── common/       # 通用组件
│   │   ├── DatabaseIcon.vue    # 数据库类型图标
│   │   └── EmptyState.vue      # 品牌化空状态（线性 SVG 图标）
│   ├── connection/   # 连接相关
│   │   ├── ConnectionDialog.vue  # 新建/编辑连接对话框
│   │   └── MigrationDialog.vue   # 数据迁移对话框
│   ├── editor/       # 编辑器相关
│   │   ├── SqlEditor.vue       # Monaco SQL 编辑器
│   │   ├── DataTable.vue       # 查询结果表格
│   │   ├── TableDetailTab.vue  # 表详情标签页
│   │   ├── SqlHistory.vue      # SQL 历史面板（收藏/标签/搜索）
│   │   ├── ErDiagramView.vue   # ER 图视图（支持 SVG/PNG 导出）
│   │   ├── ExplainTreeView.vue # 执行计划可视化树
│   │   ├── ImportDialog.vue    # 数据导入对话框（粘贴文本/文件上传）
│   │   ├── FilterBar.vue       # 结果集条件过滤器（8 操作符/AND-OR/WHERE 预览）
│   │   └── ChartView.vue       # 结果集图表视图（SVG 自绘柱/折/饼，自动推荐类型）
│   ├── layout/       # 布局组件
│   │   ├── AppHeader.vue   # 顶部导航栏
│   │   ├── SideNav.vue     # 左侧导航
│   │   ├── WorkSpace.vue   # 主工作区
│   │   ├── BottomPanel.vue # 底部面板
│   │   ├── SideProps.vue   # 右侧属性面板
│   │   ├── StatusBar.vue   # 底部状态栏
│   │   ├── TaskPanel.vue   # 后台任务面板（状态列表/回看结果/取消）
│   │   ├── ReportDialog.vue    # 定时报表订阅管理对话框（订阅 CRUD/执行记录）
│   │   └── CommandPalette.vue  # Ctrl+K 全局命令面板（命令/连接/表/历史四组）
│   └── nav/
│       ├── NavTree.vue     # 数据库对象树
│       └── NavIcon.vue     # 树节点线性 SVG 图标（按节点类型渲染）
├── layouts/
│   └── EnterpriseLayout.vue  # 企业版整体布局
├── i18n/             # 国际化（vue-i18n）
│   ├── index.ts      # i18n 实例与语言切换（localStorage 持久化）
│   ├── zh-CN.ts      # 简体中文语言包
│   └── en-US.ts      # 英文语言包
├── router/
│   └── index.ts      # 路由配置（Hash 模式 + 导航守卫）
├── stores/           # Pinia 状态管理
│   ├── auth.ts       # 认证状态（登录/角色/edition）
│   ├── connection.ts # 连接状态（当前连接/连接列表）
│   ├── editor.ts     # 编辑器状态（标签页/SQL内容）
│   ├── history.ts    # 查询历史状态
│   ├── theme.ts      # 主题状态（亮/暗/跟随系统 + 强调色预设 + 行密度）
│   ├── tasks.ts      # 后台任务状态（WS 订阅/完成通知）
│   └── ui.ts         # UI 状态（面板展开/折叠）
├── styles/
│   ├── global.css    # 全局样式
│   └── tokens.css    # Design Token（CSS 变量）
├── types/            # TypeScript 类型定义
│   ├── connection.ts # 连接相关类型
│   ├── driver.ts     # 驱动相关类型
│   ├── history.ts    # 历史相关类型
│   └── metadata.ts   # 元数据相关类型
├── utils/
│   ├── sqlFormatter.ts   # SQL 格式化
│   └── sqlLanguages.ts   # Monaco 多数据库方言注册
├── views/
│   ├── LoginView.vue     # 登录页（企业版）
│   ├── MainView.vue      # 个人版主界面
│   └── ent/              # 企业版页面
│       ├── MyDataSourcesView.vue    # 我的数据源
│       ├── EnterpriseQueryView.vue  # 企业版查询
│       ├── AiChatView.vue          # AI 对话
│       ├── ApprovalsView.vue       # 审批中心
│       ├── AuditView.vue           # 审计日志
│       └── AdminView.vue           # 管理后台
├── App.vue           # 根组件
└── main.ts           # 应用入口
```
<!-- last-updated: 2026-07-27 -->

## 核心组件说明

### SQL 编辑器 (SqlEditor.vue)

- 基于 Monaco Editor，支持多数据库方言语法高亮
- 多标签页编辑，支持 Ctrl+Enter 执行
- 自动补全（表名/列名）

### 数据表格 (DataTable.vue)

- 基于 VXE Table，支持虚拟滚动（大数据量）
- 列排序、列宽调整、单元格复制
- 列类型推断（数值/布尔/日期/JSON/文本），表头类型徽标，数值列右对齐，NULL 灰显斜体
- 表头一键切换字段名 ⇄ 字段注释（懒加载列注释，仅单表查询）
- 行高跟随密度偏好（舒适 32px / 紧凑 24px）
<!-- last-updated: 2026-07-27 -->

### ER 图 (ErDiagramView.vue)

- 基于 Vue Flow，自动布局表关系图
- 展示表间外键关系

### 导航树 (NavTree.vue)

- 树形展示：连接 → 数据库 → 表 → 列
- 右键菜单（查询/导出/查看结构）
- 节点图标为线性 SVG（NavIcon.vue，1.4px 描边，含暗色半透明底适配）
<!-- last-updated: 2026-07-27 -->

### 条件过滤器 (FilterBar.vue)

- 挂载于 SQL 编辑区与结果面板之间，折叠式条件栏
- 8 种操作符（=/<>/LIKE/>/</BETWEEN/IS NULL/IS NOT NULL），AND/OR 组合，WHERE 子句实时预览
- 应用时以原 SQL 为子查询包装过滤，可一键清除还原
<!-- last-updated: 2026-07-27 -->

### 命令面板 (CommandPalette.vue)

- 全局 Ctrl/Cmd+K 唤起，前缀路由：`>` 命令、`#` 表、`@` 连接，无前缀全部四组
- 远程搜索 200ms 防抖（表名搜索 + 历史全文搜索），↑↓/Enter/Esc 键盘导航

### 图表视图 (ChartView.vue)

- 纯 SVG 自绘柱状/折线/饼图，无图表库依赖，挂载于底部面板“图表”Tab
- 自动推断数值列（非空值 80% 可转数字）并推荐图表类型，最多取前 50 个数据点
<!-- last-updated: 2026-07-27 -->

## 状态管理

| Store | 核心状态 | 说明 |
| ------- | ---------- | ------ |
| `auth` | user, edition, isAuthenticated, isAdmin | 认证与版本判断 |
| `connection` | connections, activeConnection | 连接列表与当前连接 |
| `editor` | tabs, activeTab, sqlContent | 编辑器标签页管理 |
| `history` | records | 查询历史列表 |
| `theme` | mode, accent, density | 主题三态（亮/暗/系统）、强调色预设、行密度，localStorage 持久化 |
| `tasks` | tasks, unread | 后台任务列表与 /ws/tasks 完成通知 |
| `ui` | panelStates, bottomPanelTab | 面板展开/折叠与底部 Tab（results/chart/messages/history） |
<!-- last-updated: 2026-07-27 -->

## 路由设计

- 使用 Hash 模式（`createWebHashHistory`），兼容桌面打包 file:// 协议
- 企业版路由守卫：未登录跳转 `/login`，管理员页面校验角色
- 个人版：无路由守卫，直接展示 MainView

## 国际化（i18n）

- vue-i18n@9 组合式 API（`legacy: false`），语言包：简体中文（默认）/ English
- 语言持久化于 localStorage（键 `lyradb_locale`），未设置时按浏览器语言探测
- 顶部导航栏提供语言切换下拉；Element Plus 组件语言经 `App.vue` 的 `el-config-provider` 联动切换
<!-- last-updated: 2026-07-27 -->

## 构建配置

- **开发代理**：`/api` → `http://localhost:8080`（含 WebSocket）
- **代码分割**：monaco / element-plus / vxe-table 独立 chunk
- **构建目标**：ES2020
- **输出目录**：`frontend/dist/`
