# 系统架构设计

## 产品两大形态

LyraDB 的所有交付形态归为两大类，二者**共享同一份后端内核（Spring Boot）与前端页面（Vue 3）**，仅在「服务端部署位置」与「访问外壳」上不同：

| 形态 | 载体 | 服务端位置 | edition | 定位 |
| ------ | ------ | ----------- | --------- | ------ |
| **BS 架构版本** | 浏览器 | 独立部署（自托管/企业服务器/Docker） | personal 或 enterprise | 浏览器直接访问；个人自用或团队协作 |
| **本地客户端版本** | 安装包 | 见下 | — | 安装即用的客户端外壳 |
| ├─ 桌面客户端 | jpackage 安装包 | **内嵌本地服务端**（127.0.0.1 随机端口） | personal | 纯个人、本地直连、可离线，对标 DBeaver |
| └─ 移动客户端 | Android APK / 鸿蒙 HAP / iOS App | **连接远端 BS 服务端** | 随所连服务端而定 | 原生外壳 + WebView 加载 BS 前端 |

> 关键区别：**桌面客户端 = 自带本地服务端的客户端；移动客户端 = 连接远端服务端的客户端**。两者都是「客户端外壳 + 同一套 Web/后端内核」，差异仅在服务端在本地还是远端。移动端因此不需要在手机上运行 JDBC 驱动，天然支持全部 9 种数据库。

## 整体架构

```
┌───────────────────────────────────────────────────────────────┐
│                          客户端外壳层                          │
├───────────────────┬──────────────────────┬────────────────────┤
│  浏览器（BS版本）  │  桌面客户端(jpackage) │  移动客户端         │
│  Vue 3 + Vite     │  内嵌服务端+Vue静态   │  原生外壳+WebView   │
└─────────┬─────────┴──────────┬───────────┴─────────┬──────────┘
          │ HTTP/WS            │ 本地回环             │ HTTPS/WSS
          ▼                    ▼                     ▼
┌───────────────────────────────────────────────────────────────┐
│                        后端服务层（同一份内核）                │
│                    Spring Boot 3.2 (Java 17)                   │
│    桌面：随客户端内嵌本地运行  │  BS/移动：独立部署（个人/企业）│
├───────────────────────────────────────────────────────────────┤
│  Controller → Service → Repository                            │
│  (REST API)   (业务逻辑)  (JPA/H2 持久化)                     │
├───────────────────────────────────────────────────────────────┤
│  横切关注点：Security / CORS / WebSocket / 异常处理             │
└────────┬──────────────────────────────────┬───────────────────┘
         │ JDBC (动态驱动)                   │ SSH Tunnel
         ▼                                  ▼
┌──────────────────┐          ┌──────────────────────┐
│  目标数据库       │          │  跳板机 / 内网数据库  │
│  MySQL/PG/Oracle │          │  (Apache MINA SSHD)  │
│  ClickHouse/...  │          └──────────────────────┘
└──────────────────┘
```

## 分层结构

### 后端分层

| 层 | 包路径 | 职责 |
| ---- | -------- | ------ |
| Controller | `io.github.lexaquila.lyradb.controller` | REST 端点定义、请求校验、响应封装 |
| Service | `io.github.lexaquila.lyradb.service` | 核心业务逻辑、事务管理 |
| Repository | `io.github.lexaquila.lyradb.repository` | 数据访问（Spring Data JPA） |
| Model/Entity | `io.github.lexaquila.lyradb.model.entity` | JPA 实体映射 |
| Model/DTO | `io.github.lexaquila.lyradb.model.dto` | 数据传输对象 |
| Config | `io.github.lexaquila.lyradb.config` | 应用配置（安全/CORS/WS/启动引导） |
| Driver | `io.github.lexaquila.lyradb.driver` | JDBC 驱动动态下载与类加载 |

### 前端分层

| 层 | 目录 | 职责 |
| ---- | ------ | ------ |
| Views | `src/views/` | 页面级组件（路由入口） |
| Components | `src/components/` | 可复用 UI 组件 |
| Stores | `src/stores/` | Pinia 全局状态管理 |
| API | `src/api/` | Axios 封装，后端接口调用 |
| Types | `src/types/` | TypeScript 类型定义 |
| Utils | `src/utils/` | 工具函数（SQL 格式化等） |
| Router | `src/router/` | 路由配置与导航守卫 |

## 数据流

> 下述数据流按 **edition** 区分，与客户端形态无关：无论是浏览器、桌面内嵌服务端还是移动端 WebView，前端→后端的链路一致。移动端仅多一层原生外壳（服务端地址管理/会话存储/生物识别）。

### personal 数据流

```
用户操作 → Vue 组件 → Pinia Store → API 层 → 后端 Controller
    → Service → JDBC 连接目标 DB → 返回结果 → 前端渲染
```

### enterprise 数据流

```
用户登录 → AuthController → Spring Security Session
    → 后续请求携带 Session Cookie
    → Controller 校验角色权限
    → Service 执行（含审计日志记录）
    → 敏感操作触发审批工作流
```

### 移动端接入流（外壳层）

```
App 启动 → 选择/填写 BS 服务端地址（个人自托管 或 企业）
    → 生物识别解锁 → WebView 加载 BS 前端
    → 后续完全走上述 personal / enterprise 数据流
```

## 发行版门控机制

通过 `app.edition` 配置项（环境变量 `LYRADB_EDITION`）实现同一代码库支持两种发行版（BS 与本地客户端共用）：

- **personal**（默认）：Security 全部放行，无登录页，直接展示主界面
- **enterprise**：启用 Spring Security 认证、RBAC 角色校验、审批流、审计日志

关键门控点：

- `SecurityConfig`：根据 edition 决定是否启用认证
- `EnterpriseBootstrap`：企业版启动时初始化管理员账户和默认策略
- 前端 `router/index.ts`：根据 `/app/info` 返回的 edition 决定路由模式

## 驱动管理机制

```
drivers.json（驱动注册表）
    → DriverService 解析可用驱动列表
    → Maven Resolver 从远程仓库下载 JAR
    → 缓存至 ~/.lyradb/drivers/
    → 自定义 ClassLoader 隔离加载
    → WebSocket 推送下载进度
```

## 安全架构

| 组件 | 说明 |
| ------ | ------ |
| Spring Security | 企业版 Session 认证 + HTTP Basic |
| BCrypt | 用户密码哈希存储 |
| Jasypt | 数据库连接凭据 AES 加密 |
| RBAC 角色 | 五角色：`PLATFORM_ADMIN`（平台管理）/ `DS_ADMIN`（数据源管理）/ `STEWARD`（数据管家）/ `ANALYST`（分析师）/ `AUDITOR`（审计员） |
| 审批工作流 | 敏感操作（DDL/导出）需审批通过后执行 |
| 审计日志 | 记录所有查询操作（用户/SQL/时间/结果） |
| 元数据防注入 | 驱动层标识符/字面量转义校验（SQLite/ClickHouse/MaxCompute） |

企业版放行端点仅限 `/app/**`、`/auth/login`、`/auth/logout`、`/drivers/**`、`/ws/**`；不再放行 H2 Console 与 Swagger，其余端点均需认证。`/admin/**` 管理端点在控制器内进一步校验 PLATFORM_ADMIN/DS_ADMIN 角色。移动客户端经 WebView 访问同一套端点，鉴权与 BS 版本完全一致。
<!-- last-updated: 2026-07-27 -->
